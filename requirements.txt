# Invariant Hunt — core dependencies
numpy>=1.26
scipy>=1.11
matplotlib>=3.8
pandas>=2.1
tqdm>=4.66
omegaconf>=2.3.0
pyyaml>=6.0
pydantic>=2.0
optuna>=3.5.0
tabulate>=0.9.0

# Full conduit / TOE reproduction (optional but recommended)
torch>=2.1.0

# Distributed sweeps (optional)
# ray>=2.9.0

# Dev / test
pytest>=8.0
pytest-cov>=5.0
